values
{
	DELTA_T ( 1e-5 );
	RESOLUTION ( 32 64 128 );
	solver ( interFoam interIsoFoam );

    // Fixed parameters for job script setup
    MESHER              ( blockMesh );
    N_CORES_TOTAL       ( 4 );
}
